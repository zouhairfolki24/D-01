#include <stdio.h>

int affiche_tableau(int size_tableau) {
    int tableau[size_tableau];

    for (int i=0; i<size_tableau; i++) {
        tableau[i] = 8;
    }

    return tableau[size_tableau];
}

int main() {
    int integers[] = affiche_tableau

    return 0;
}