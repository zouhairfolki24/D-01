#include <stdio.h>
#include <string.h>

int main()
{
    char characters[6];

    for (int i = 0; i < strlen(characters); i++)
    {
        printf("Entrer une character: ");
        scanf("%s", &characters[i]);
    }

    for 

    return 0;
}