#include <stdio.h>
#include <stdlib.h>

int main() {
    char *titres = (char*) malloc(100 * sizeof(char));

    for (int i = 0; i < n; i++) {
        printf("Entrez l'element %d: ", i + 1);
        scanf("%d", &tableau[i]);
    }

    return 0;
}