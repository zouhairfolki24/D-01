#include <stdio.h>
#include <string.h>

int main()
{
    char name[] = "Zouhair";
    printf("%zu\n", sizeof(name));
    printf("%zu", strlen(name));

    int a;
    printf("%zu", sizeof())

    return 0;
}